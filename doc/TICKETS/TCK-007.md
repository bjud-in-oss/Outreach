# TCK-007: UI & DASHBOARD-ÖVERVAKNING AV SERIELL MOTOR

- **Status**: OPEN (Väntar på initiering)
- **Domän**: `src/features/gemini_live_swarm/`
- **Omfattning**: Uppdatera dashboards, sidopaneler och styrkort för övervakning och jämförelse av SERIELL_MOTOR mot övriga krafter.

## Krav & Artefakter
- Uppdatera `SwarmDashboard.tsx` för visualisering och jämförelse av `SERIELL_MOTOR`.
- Uppdatera `TelemetrySidebar.tsx` för att visa realtidsmätning av alla 4 krafter.
- Uppdatera `MasterDevelopmentPlan.tsx` för att reflektera SI v10.0-status och nya tickets.
- Verifiera via `pnpm test` och `pnpm verify`.
